# Ghostty configuration for the showcase recording.
# Start with:  ZDOTDIR=$PWD/showcase/env open -na Ghostty --args --config-file=$PWD/showcase/env/ghostty-demo.conf
# (a second Ghostty instance, so your everyday windows keep their own config)

theme = tokyonight
background-opacity = 1

font-family = "Hack Nerd Font Mono"
font-size = 20

window-padding-x = 18
window-padding-y = 18
window-theme = ghostty
window-decoration = true
# no forced title: the shell sets it to the directory and nvim to its own, which the
# daemon's title heuristic and the recording rely on
confirm-close-surface = false
resize-overlay = never
macos-titlebar-style = tabs

cursor-style = block
cursor-style-blink = false
shell-integration-features = no-cursor

# The demo shell: a plain zsh reading showcase/env/.zshrc (via ZDOTDIR).
command = /bin/zsh -i
