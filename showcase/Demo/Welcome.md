# zmk-vim-mode showcase

A small vault for the **zmk-vim-mode** showcase. Vim key bindings are on, and the
keyboard follows the editor: `i` puts it in its INSERT layer, `Esc` brings the
NORMAL layer back, `:` opens the command line on the CMDLINE layer.

- [[Romak layout]] — the alpha layout the Diamond types with
- [[Vim mode]] — how the host tells the keyboard which mode it is in
- [[Tasks]] — a checklist to edit on camera

> Everything in this vault is synthetic showcase content.
